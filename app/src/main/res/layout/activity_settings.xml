<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/white"
    tools:context="com.adguard.webbatterytester.SettingsActivity">

    <!-- We use a Toolbar so that our drawer can be displayed in front of the action bar -->
    <android.support.v7.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="4dp"
        android:background="?attr/colorPrimary"
        android:elevation="16dp"
        android:minHeight="?attr/actionBarSize"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
        android:title="@string/app_name" />

    <include layout="@layout/preference_item_text" android:id="@+id/repeat_settings"/>
    <View style="@style/settingDivLine" />
    <include layout="@layout/preference_item_text" android:id="@+id/domain_settings"/>
    <View style="@style/settingDivLine" />
    <include layout="@layout/preference_item_text" android:id="@+id/reset_settings"/>
    <View style="@style/settingDivLine" />

</LinearLayout>
